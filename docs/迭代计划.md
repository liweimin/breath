# 迭代计划

## P0（近期优先）
1. 修正 `scripts/generate-audio-data.ps1` 输出 MIME 为 `audio/mpeg`。
2. 统一清理呼气旧资源（评估是否移除 `assets/audio/exhale-light-short.ogg`，避免误用）。
3. 增加“设置参数后的一键预设”能力（如 4-4-4-4、4-7-8）。

## P1（体验优化）
1. 增加“阶段标签更明显”的视觉提示（文字 + 颜色层级）。
2. 优化移动端按钮触达与字号（尤其小屏 Android）。
3. 增加导出完成提示细节（导出耗时、文件大小）。

## P2（稳定性与维护）
1. 建立基础回归检查清单（播放、暂停、导出、移动端验证）。
2. 文档迭代机制固定化：每次发布后更新 `docs/项目现状.md` 与 `docs/迭代日志.md`。
3. 考虑引入轻量版本号展示（页面底部显示 commit short hash）。

## 验收标准（下一轮）
- 手机主流浏览器（Chrome/系统浏览器）呼气提示稳定可听。
- 导出完整音频可成功下载且时长与设置匹配。
- Vercel 自动部署链路保持稳定（push 到 `main` 后自动 `Ready`）。

