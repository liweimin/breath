# 迭代日志

## 2026-02-19

### SEO 技术基础（本轮）
- 补全页面 SEO 元信息：`title`、`description`、`canonical`、`robots`、OG、Twitter Card。
- 增加 `JSON-LD` 结构化数据（`WebApplication`）。
- 新增 `robots.txt` 与 `sitemap.xml`，供搜索引擎抓取与发现页面。
- 增加隐藏 `H1`（`sr-only`）优化页面语义结构。

### UI 入口简化（本轮）
- 移除顶部“呼吸冥想引导”说明卡，首屏直接进入功能入口。
- 将 `rhythm-summary` 挪到控制卡片中，保留参数摘要能力。
- 联系邮箱移至页面底部，减少对主操作区的干扰。
- 本轮未改动动画逻辑与核心功能逻辑。

### 预设与内容区（本轮）
- 新增四个预设入口：`平衡 4-4-4-4`、`放松 4-7-8`、`节律 6-0-6-0`、`自定义`。
- 预设支持状态同步、练习中锁定、本地持久化。
- 新增“预设说明与常见问题”折叠区，包含 FAQ、权威参考链接与免责声明。
- 增加 `FAQPage` 结构化数据，补充可索引内容。

### 页面故事与布局修正（本轮）
- 内容区新增“项目由来”说明（Vibe Coding + 冥想场景）。
- 底部联系区新增 GitHub 开源地址链接。
- 修正 PC 端网格布局：说明内容固定在动画卡片下方，解决左右高度差导致的中间留白不协调问题。

### 极简交互与显示精简（本轮）
- 左侧改为单一图标按钮控制（播放/停止），去除可见的开始/暂停/继续/停止按钮组。
- 移除“当前预设/当前参数”大段摘要文案，减少干扰信息。
- 圆球中心改为秒级数字倒计时（`1,2,3...`），并降低透明度以突出球体涨缩动效。
- 总剩余改为动画底部叠加进度条（视频播放条风格）。
- 移除静音配置项；下载逻辑保留但入口按钮隐藏，便于后续快速恢复。

### 遗留问题记录（本轮）
- 新增一项低概率问题：播放音频偶发随机慢 1-2 秒。
- 当前判断与浏览器音频上下文恢复、采样首次解码及定时器抖动有关，已记录到 `docs/已知问题.md` 以便后续专项优化。

## 2026-02-18（整理记录）

### `bc2d140` feat: 初始化呼吸冥想网页与导出功能
- 建立静态网页结构（`index.html` + `style.css` + `app.js`）。
- 实现呼吸阶段循环、实时动画与基础声音引导。
- 实现整段音频导出（WAV）与下载流程。
- 接入 Vercel 静态部署配置（`vercel.json`）。

### `8688e61` docs: 优化使用说明并添加联系邮箱
- 首页顶部说明文案改为操作步骤式描述。
- 增加联系邮箱 `leevimin@gmail.com`。
- 补充邮箱链接样式，优化可读性。

### `939e951` fix: 呼气音改为MP3提升移动端兼容性
- 呼气采样路径改为 `assets/audio/exhale-light-short.mp3`。
- 生成内嵌音频脚本输入源同步切换到 MP3。
- 目标是提升移动端浏览器（尤其非标准内核）兼容性。

## 当前结论
- GitHub 与 Vercel 自动部署链路已跑通。
- PC 与手机 Chrome 验证通过。
- X 浏览器兼容性问题暂时记录，不阻断主流程上线。

