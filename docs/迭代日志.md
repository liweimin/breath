# 迭代日志

## 2026-02-19

### UI 入口简化（本轮）
- 移除顶部“呼吸冥想引导”说明卡，首屏直接进入功能入口。
- 将 `rhythm-summary` 挪到控制卡片中，保留参数摘要能力。
- 联系邮箱移至页面底部，减少对主操作区的干扰。
- 本轮未改动动画逻辑与核心功能逻辑。

## 2026-02-18（整理记录）

### `bc2d140` feat: 初始化呼吸冥想网页与导出功能
- 建立静态网页结构（`index.html` + `style.css` + `app.js`）。
- 实现呼吸阶段循环、实时动画与基础声音引导。
- 实现整段音频导出（WAV）与下载流程。
- 接入 Vercel 静态部署配置（`vercel.json`）。

### `8688e61` docs: 优化使用说明并添加联系邮箱
- 首页顶部说明文案改为操作步骤式描述。
- 增加联系邮箱 `leevimin@gmail.com`。
- 补充邮箱链接样式，优化可读性。

### `939e951` fix: 呼气音改为MP3提升移动端兼容性
- 呼气采样路径改为 `assets/audio/exhale-light-short.mp3`。
- 生成内嵌音频脚本输入源同步切换到 MP3。
- 目标是提升移动端浏览器（尤其非标准内核）兼容性。

## 当前结论
- GitHub 与 Vercel 自动部署链路已跑通。
- PC 与手机 Chrome 验证通过。
- X 浏览器兼容性问题暂时记录，不阻断主流程上线。

